# on kali linux
# place this file in ~/.msf4
# run apt install -y libsqlite3-dev libpcap-dev libpq-dev oracle-instantclient-devel
# run bundle --install path=vendor

source 'https://rubygems.org'

gem "metasploit-framework", path: "/usr/share/metasploit-framework"
